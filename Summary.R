library(shiny)
library(shinydashboard)
library(ggplot2)
library(reshape2)


SummaryUI <- function(id, label = "summary"){
  fluidPage(
            # First Section
            fluidRow(
                     h2("Summary")
            )

  )
}
